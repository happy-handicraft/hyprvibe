# Shared Beads Git hooks

These hooks are generated and maintained with `bd hooks install --shared`.

After cloning this repository, enable them with:

```sh
git config core.hooksPath .beads-hooks
bd hooks list
```

Run `bd hooks install --shared` to regenerate the hooks after a Beads upgrade.
