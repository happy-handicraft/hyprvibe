# Beads Configuration File
# This file configures default behavior for all bd commands in this repository
# All settings can also be set via environment variables (BD_* prefix)
# or overridden with command-line flags

# Enable JSON output by default
# json: false

# Default actor for audit trails (overridden by BD_ACTOR or --actor)
# actor: ""

# Integration settings (access with 'bd config get/set')
# These are stored in the database, not in this file:
# - jira.url
# - jira.project
# - linear.url
# - linear.api-key
# - github.org
# - github.repo

# Dolt is the source of truth. Sync it explicitly with `bd dolt pull` and
# `bd dolt push`. The legacy beads-sync Git branch is retained as an archive.
sync.remote: "https://github.com/ChrisLAS/hyprvibe.git"
federation.remote: "git+https://github.com/ChrisLAS/hyprvibe.git"

export.git-add: false